# Pi prompts

This directory contains documentation for optional Pi prompt templates.

Actual prompt templates belong in `prompts/templates/`, which is the directory registered in `package.json`. Keeping documentation here prevents `README.md` from being exposed as a `/README` prompt.

The plugin's primary workflows are implemented as Pi extensions, commands, and skills.
