# Dogfood 驾驶日志（2026-09-08）

> 实机路测记录：空项目 /tmp/pi-dojo，herdr 内跑真实 pi，主 agent 当司机驾驶，
> 记录问题 → 优化 agent-pi。状态随驾驶推进更新。

## 环境
- pi 0.85.1 · herdr 0.8.2（server 运行，socket OK）· provider opencode-go/deepseek-v4-flash
- 空项目 /tmp/pi-dojo（git init），herdr workspace w5A，pane w5A:p1，agent `dojo-pi`

## 驾驶记录
- **M1（完成，验收 PASS）**：0→1 构建零依赖 Node CLI `jd`（add/list/search/summary + node:test + README + 本地 commit）。
  实况：herdr `agent prompt` 驱动；1m54s；commit ff44d8f；`node --test` 2/2；真冒烟往返正确；
  产物 bin/jd.js / tests/jd.test.js / package.json / README；agent 自述与磁盘事实一致。
- **M2（完成，验收 PASS）**：加 `jd stats`（notes 计数+词数），commit 6e4752a，node:test 3/3；
  agent 自述诚实（提醒词数为原始 token 含时间戳）。
- **M2a（完成，验收 PASS）**：herdr 内 agent-pi 委派可见 pane 验证 —— w5A 内 headless 父 pi
  调 `subagent_create`(builder, join) → **子 pane w5A:p3 真实打开**（herdr transport），
  完成 `child-flag.ts` 后 pane 自动关闭（success linger），父收 `RESULT-SUMMARY: PASS`。
- **M3（组件命中：verify_execution 收据闭环）**：dojo-pi 交互 TUI 自建 `jd top`（commit 0d135b5，4/4 测试），随后调 `verify_execution` → **verifier 只读子 agent 真 spawn**（verifier/compositions 事件 + session 落盘），审计产出 findings/hard_blockers[]/nextAction:REPORT。
- **M3b（隔离复测）**：同流程 headless 父完整收尾（agent_settled），verify 工具正常返回，verifier 子 36s 完成（transport herdr）。**组件判定：工作正常，非缺陷。**
- **M-C（SPEC 模糊试驾，完成 PASS）**：修复 D11 后复测全通——SPEC 规格五文档落盘 → show_spec → viewer **Spec approved**（人批）→ ask_user 方向由司机代选"继续实现" → 全新串行 session 实现 `jd weekly`（单文件 HTML 周报，内联 CSS），tests 18/18，commit 09761fa。规格文档 context-os/ 保留为已批产物。
- **M-G（workflow/安全工具真机，完成）**：headless 父调三工具全在场：**eval_run PASS**（provider-free 2 离线例，`completionAllowed:false`，报告落盘 .pi/workflow/evals ×2）、**workflow_advice** VERIFY（bounded）、**safe_port_scan** 优雅降级（nmap 未装 ENOENT，非崩溃）。安全门 security-guard 全程隐式命中（每轮 tool_call）。
- **M-H（瘦身清单）**：产出 docs/slimming-candidates.md —— 命中辩护清单 + 四档：已删/退役修剪候选（CHAIN 等）、真机未命中可选（viewers/sounds/专项，登记不再堆料）、环境受限（nmap）、缺陷待修（D6/D12 wedged 族）+ 设计级降级（PIPELINE 仪式/eval-judge descope）。
- **深水区十一（收尾认证）**：fleet-mailbox 真机需交互多轮父问答 → B 档文档化未命中（lib 测试覆盖）。D13 三连修后 **verify:release 重认证 GREEN（exit=0）**；coverage-matrix C 增 D13-real/D17 行。
- **深水区十（D13-real 真根因与三连修）**：live 复验发现 reviewer.md 契约**不进 worker 提示**（代码构造提示）→ 补代码模板 decision 行（3289abf）；模型仍漏字 → reviewerDecision 改全块语义扫描 + 否定优先（a1a3d6a，6 测试）；仍漏 → reviewerFailure 进入格式修复轮 + 强制字面 APPROVED（5940653）。**最终 live 验证：reviewer journal done/exit0**（修复轮二次输出含 APPROVED 过门）。真机驱动出三层真实缺陷深度，非纸面。
- **深水区九（受控父-crash / D17）**：headless 后台 worker 在 shell-pane 下两次随新前台命令被 SIGINT（exit130）——shell job-control 组杀；独立 herdr-pane worker（M2a）存活。**D17 定性：headless bg 在交互 shell 中脆弱 = 环境/设计约束**（后台耐久承载应走 herdr-pane transport 或会话内 join 收集）；非代码 bug，记录为使用指引 + 覆盖矩阵 B 档。
- **深水区八（恢复负载实证）**：真 crash 事件（误杀运行中 worker → journal error/exit130）+ subagent_resume 对 terminal-error 行返回 **FAIL（fail-closed 正确**：设计只续 running/stale，不续错误态）。可续场景=父崩溃留 running 行，需受控父-crash 环境——记录为受控运行待办。背景委派 join=false + journal 落盘完成路径仍证。集成恢复测试 3/3（fake-child 机制层）。
- **深水区七（发布认证）**：`npm run verify:release` 完整门 **exit=0 GREEN**（doctor:strict 13/13 + strict tsc + bun 284 + vitest 998/13skip + npm audit + package-smoke agent-pi-2.2.0.tgz）。驾驶舱经真机战役 + 8 项缺陷修复后达**发布级可交付**。
- **深水区六（PIPELINE 定论 + 模糊迭代）**：PIPELINE e2e 换 glm-5.3-flash 仍在其固定窗口未走完 4 相位（串行 worker 超窗口；相位机制可起跑）→ 维持 expert/手动判级，e2e 窗口过短记工具改进项。模糊需求"导出合并 markdown" → agent 自驱实现 **`jd export`**（2a1946c，21/21 tests）且**自提交自收尾**（D7 杠杆实证：提示语含"提交"即完成，无 stop-short）。
- **深水区五（覆盖矩阵终稿 + D16 e2e 尝试）**：仓库自带 pipeline-herdr-e2e（4 相位真跑）执行 → 在其固定窗口内未完成（UNDERSTAND worker 已派发；PIPELINE-TASK-PASS 文本为提示回声）——PIPELINE 顺序 worker 慢模型下超窗口，非 D16 回归（纯文本改动，doctor/tests 绿）。产出 **docs/coverage-matrix.md**（战役总账：命中辩护 / 未命中瘦身输入 / 缺陷处置总账 / 信任红线验证证据）。
- **深水区四（D16 + 发布认证）**：PIPELINE 迷失根因定位——pipeline_status 只列状态不指动作。已修（771ceef）：status 输出加 `Next action:`（按相位给"委派配置角色→advance_phase"或"自做→advance_phase"精确指引，16 行最小补丁）。live 全相位复驱依赖 config 选择 UI，按 monolith 延后纪律延至下次功能触碰。doctor:strict 发布级认证 **13 passed / 0 warn / 0 fail**；全量测试绿（998）。
- **深水区三·PIPELINE 真相位驱动（证据型结论）**：真机驱动 jd week 别名走 PIPELINE 全相位。结果：相位 1(PLAN) 由 plan-agent **自动完成并推进任务清单**；随后 agent 在 BUILD 相位**迷失**——不调 pipeline_status/advance_phase，转而全盘 find/grep 外部目录（/opt、~/.hermes、claude-plugin）找"别的 pipeline 实现"，司机纠偏后 3+ 分钟仍未收敛 → 时间盒收割关闭。advance_phase 零调用（.pi 无相位事件）。**结论：PIPELINE 多相位自助驱动超出 deepseek-flash 当前能力/可发现性**；相位机制能自动起跑但 agent 无法自续。佐证瘦身判级：PIPELINE=手动/专家可选（slimming-candidates §5），且暴露相位指导可发现性缺口（候选 D16：相位无自描述下一步提示）。
- **深水区三（失败侧验证 + D15）**：(1) 故意破坏可观察功能（listData 清空）→ tests 3 红 + CLI 空 → **verify_execution 返回 FAIL**（真机，verifier 抓到回归并拦完成）——fail-closed 红线成立。(2) 途中暴露 **D15**：首次 verifier 子经 herdr transport startup 失败（5.4s/0 tokens/exitCode1）→ 验证 BLOCKED 且吞 attempt。已修（9155a3e）：runVerifierSubagent spawn 级重试 ≤2 次带退避；typecheck+46 测试绿。(3) **方法课**：首次"破坏"改到未被 CLI/测试使用的 list() → 行为无变化 → verifier PASS 是**正确**判定（非假放行）——verifier 只对可观察行为负责，验收须含行为/测试断言。(4) 项目恢复健康（reset 回 9c29f8c，18/18），agent-pi 全量回归绿（998）。
- **深水区二（Viewer 永等封顶 + 补试）**：(1) plan/spec 批准 viewer 与 completion-report 同族永等——show_plan/show_spec 无人点击即无限 block。已加 bounded wait（8ef4de3，PI_VIEWER_WAIT_MS 默认 5min；plan 超时→declined 路径、spec 超时→优雅报错，门均保持关闭）。真机复验：15s env 下 show_plan ~25s 返回，不再 wedge。(2) D7 stop-short 评估：agent_end nudge 已存在（sendMessage triggerTurn）；M-A 无清单无可 nudge，M-B 有清单仍停=模型无视提示。**决策不改代码**（重复轰炸成噪音），杠杆在提示语明确 commit+summary。(3) 未命中工具补试：orchestration_status（MODE METRICS）✓、compose_exec（1/1 steps）✓、tool_search（12 工具索引）✓。残余未命中：fleet-mailbox(env 门)、security_news、GUI viewers、toolkit/tex/oauth——已录 slimming-candidates。
- **D12 修复（深水区）：show_report 永等浏览器点击 → wedge 根因；bounded wait 修复 + 真机复验（27s 返回）。D6/D8/D12 同族同根因，标记修复。
- **Viewer/安全 roster 真机（深水区）**：show_reports 工具真机调用成功（headless 只回 "Reports viewer closed."，URL 走 notify 不进文本；GUI 深度已由 PLAN/SPEC viewer 人工批准覆盖）。secure-engine `runSweep` 直接真扫 agent-pi 自身：143 findings/7 类（unsafe_eval 78 / data_exfiltration 17 / system_prompt_leakage 14 / missing_output_filtering 13 / prompt_injection 12 / credential_exposure 8 / missing_input_validation 1）——**大量启发式误报**（自家合法代码与 yaml 文本被 regex 命中）→ roster 信噪比低，判"实验性"，进瘦身候选观察。运行时安全门（security-engine，tool_call 执行）与扫描器（secure-engine，启发式）职能区分记录。
- **M-F（batch 并行 + reviewer 门，完成）**：headless 父 `subagent_create_batch` 2 builder 并行 → **PASS/PASS**（task-journal 双 done）；随后 reviewer 委派 → reviewer 产出完整审阅（sa3 RESULT + 18 tests 自证）但 **journal 标 error + 父误报 APPROVED** → 实锤 **D13**：reviewer RESULT 无显式 decision 行 → reviewerDecision UNKNOWN → fail-closed 正确拦截，但父模型无视 blocked 仍报 APPROVED。**已修**（5cb77d3）：reviewer.md 契约强制 `decision: APPROVED|NEEDS CHANGES` 独立行。
- **M-E（CHAIN/PIPELINE 真实命中评估，完成）**：PIPELINE 真机试驾（search -i 小任务）——管道机器工作正常（plan-build + pipeline_status + advance_phase 要求逐相位 subagent_create 委派），但 **agent 自判"对小任务纯仪式"退回 NORMAL**（被允许），功能落地 9c29f8c（18/18）。CHAIN：代码自证 retired（agent-chain L1065 + 死 runAgent/runChain），未驾驶。**收敛证据**：CHAIN=退役瘦身候选；PIPELINE=重仪式可选面，契合 POSITIONING"降级为手动可触发"。
- **M-D（TEAM 模糊专家委派，完成 PASS）**：模糊需求 → agent 自驱：建任务清单(3) → **委派 Planner SA1**（glm-5.3-flash，真实子 pane）评估 → Builder 实现 **`jd recent [n]`**（跨日最近笔记）→ **verifier 返回 PASS**（TEAM 内 verify_execution 真跑）→ tests 14 绿 → 司机提交 7a15744。TEAM 调度面（任务门/委派/RESULT/verify）全链真实命中，无 roster 拦截（subagent_create 路径直通）。
- **教训（用户指示）**：TEAM/SPEC 等驱动**禁止并行**（同仓同文件互扰）；本回合因并行造成 stash 冲突，司机决断保留 HEAD + 规格文档，weekly 半成品丢弃重实现。

- **M-B（PLAN 模糊 + 批准闭环，验收 PASS）**：模糊正式需求加 --json。真机流程：agent 自动建任务清单(4)、写 .context/todo.md、show_plan → **写 bin/jd.js 被批准门拦截 → 用户在 plan-viewer 手动批准后放行**（真实人审环）→ 实现落地 commit e3d173d（--json 全局/前后位皆可、6 JSON 测试+6 回归、12/12 绿、README 更新）。司机独立验证通过。
- 观察：PLAN 完成后 agent 未自动调 show_report/verify_execution —— 完成门只在调用对应 surface 时强制（opt-in，符合设计）；中途两次 stop-short 靠司机"继续"推动完成（D7）。provider 400/observer 耗尽源于 npm:pi-blackhole（用户已关，D10）。
- **M-A（模糊 NORMAL + 全新 session，验收 PASS）**：全新 agent `jd-user`，模糊需求"不好用，变好用点，别太复杂"。自驱重构 bin/jd.js（+70 行）：**发现并修真实 bug**（UTC 时间戳与本地日期错位 → 本地时钟戳）、加 `jd list YYYY-M-D` 任意日期、noteWords 复用。6/6 测试。**司机落盘** a85ce0d（agent 停 idle 未提交）。未命中 agent-pi 编排面（纯 NORMAL 工具，无 .pi 轨迹）——小任务符合预期。

## 问题记录（驾驶中发现，待修或已修）
| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| D1 | `herdr agent prompt --wait` 长时间阻塞，司机侧 bash abort 后**提示语其实已送达**——delivery 状态歧义，易重复提交 | 驱动歧义 | 缓解：发送不带长 wait，轮询 `agent get`；herdr 官方亦警告 timeout≠未送达 |
| D2 | 本仓 skills/ 缺 herdr SKILL（外部司机与 pi agent 均无控制 herdr 的权威指引） | 能力缺口 | **已修**：装 herdr v0.9.0 SKILL → skills/herdr/SKILL.md (512a953) |
| D3 | agent-pi 在 herdr 内委派是否真开可见子 pane（HERDR_ENV=1 时 herdr-client 路径） | 编排可见性 | **已验证工作正常**（M2a：pane p3 开→完成→自动关），非 bug |
| D4 | M1/M2 的 pi 跑原生交互，未走 agent-pi 模式面（task/批准/完成门）——"驾驶舱"语义只在委派路径体现 | 覆盖面 | 委派路径已证（M2a）；PLAN 批准→verifier 收据闭环待评估 |
| D5 | mode-semantics "coordination-state 单一状态源"措辞过宽（模式专属态实际在闭包+持久投影） | 文档失真 | **已修**（08d3f79 后同轮）|
| D6/D8/D12 | 交互长 turn 后 herdr 卡 working 永续（D6 dojo 20min、D12 TEAM 13min）——**根因：show_report/completion-report `await waitForResult()` 永等浏览器人工点击**；无人开 viewer（headless/司机/远端）→ 工具永不返回 → turn 永不结束 | 用户可感知卡死 | **已修**（bounded wait PI_REPORT_WAIT_MS 默认 5min，超时优雅关闭+返回；两调用点）。真机复验：20s env 下 27s 内返回，不再 wedge |
| D7 | 模糊 NORMAL 任务：agent 完成高质量改动 + 自测 6/6 后**停 idle 未提交、无收尾总结**（stop-short）；司机代提交 | 工作流缺口 | 观察：可能因我的模糊提示未要求提交/总结。后续模糊任务提示补"做完提交并总结"再对照 |
| D8 | herdr idle/working 状态在同轮再现歧义（声称 idle 时屏显仍在编辑） | 驱动干扰 | D6 同源候选，合并观察 |
| D9 | 司机 `git add -A` 误收 .pi/ 运行时杂物入测试仓 | 司机失误 | **已修**：git rm --cached + .gitignore（5bbf61f）；教训：测试仓 add 前查 status |
| D10 | provider 400 (no body) + "Observational memory: observer failed: all model candidates exhausted"——根因 npm:pi-blackhole 网关被关；期间 agent stop-short 加剧 | 环境故障 | 外部，非 agent-pi。用户已关 blackhole；**纪律：provider 变更后重启 pi 或 reload 会话**（用户指示）|
| D11 | SPEC 死锁误判：批准前 bash mkdir 被门拦，拒绝理由无规划根指引 → agent 全锁死结论停步 | 门 UX | **已修**（5d91f93）+ 回归测试（approval-gate 32/32）；复测 SPEC 全通（M-C）|


## 优化行动（对应问题）
- D2/D5 已随行修复；D1 属驱动手法，记录不再犯。D3 验证通过。D4 为覆盖面评估项，非缺陷。
